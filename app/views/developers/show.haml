%body

  = render :partial => 'shared/header'

  #main
    #path
      You are here:
      = link_to 'Home', root_url
      >>
      %h1 API Key
  
    #content
      #reports.container_12
        .block
          .block_head
            %h2 API Key
          .block_content
            %p
              Your API Key is: 
              %b
                #{@developer.api_key}
            %p
              We have also sent you an e-mail with your API Key as well.  Now you can start working with the 
              %a{ :href => "/api" }
                Oil Reporter API
              .
        .clear

  = render :partial => "shared/footer"
